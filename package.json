{
  "name": "zydy",
  "version": "1.0.0",
  "private": true,
  "description": "Overblikssiden på zydy.dk — links til familiens apps.",
  "type": "module",
  "scripts": {
    "deploy": "npx --yes wrangler@4 deploy",
    "dev": "npx --yes wrangler@4 dev",
    "serve": "python3 -m http.server 4175 -d public",
    "ideer": "node scripts/ideer.mjs"
  }
}
